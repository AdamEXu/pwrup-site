{
    "buildCommand": "pnpm build",
    "devCommand": "pnpm dev",
    "installCommand": "pnpm install",
    "framework": "astro",
    "functions": {
        "dist/server/entry.mjs": {
            "runtime": "nodejs20.x"
        }
    }
}
